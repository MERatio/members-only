<% provide(:title, @user.name) %>
<div class="row">
  <div class="col-md-4">
    <h1><%= @user.name %></h1>
    <%= button_to 'Create a post', new_post_path, method: :get,
        class: 'btn btn-primary' %>
  </div>
</div>